{
  "buildCommand": "npm run build",
  "installCommand": "cd ../fhevm-sdk && npm install && npm run build && npm pack && cd ../nextjs && npm install --legacy-peer-deps --no-package-lock ../fhevm-sdk/@fhevm-sdk-0.1.0.tgz && npm install --legacy-peer-deps"
}
